# @tiptap/extension-mathematics peers on katex@^0.16.4, but the reader uses
# katex@0.17 (rehype-katex). The APIs are compatible, so accept the resolution
# rather than pin an old katex. Keeps `npm install` (incl. Vercel CI) green.
legacy-peer-deps=true
